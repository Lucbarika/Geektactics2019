<%= simple_form_for([ course, tool ], remote: true) do |form| %>
  <%= form.input :name %>
  <%= form.input :description %>
  <%= form.input :pdf_url %>
  <%= form.input :audio_url %>
  <%= form.input :price %>
  <%= form.button :submit %>
<% end %>
