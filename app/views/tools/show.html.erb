<div class="tool-show-container">
  <div class="tool-name">
    <h1><%=@tool.course.title.capitalize%> - <%=@tool.name.capitalize%></h1>
  </div>
  <div class="tool-details-container">
    <div class="tool-description">
      <p><strong>Category: </strong> <%=@tool.course.category.capitalize%></p>
      <br>
      <br>
      <p><strong>Description: </strong> <%= @tool.description %></p>
    </div>
    <div class="support-container">
    <div id="tool-video">
      <h4>Video: </h4>
      <div class="tools-video">
        <iframe width="649" height="365" src="<%= @tool.video_url %>" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
      </div>
    </div>
    <div class="tool-pdf">
      <h4>PDF: </h4>
      <p>Download Course PDF: </p>
      <p class="fas fa-file-pdf"></p>
       <%= @tool.pdf_url %>
    </div>
    </div>
  </div>
<!-- Code for displaying al the tools feedback toDo for later -->
<!--   <div id="tool-price">
    <strong>Price: </strong> <%= @tool.price %> CAD
  </div>
  <div id="add-to-purchase">
    <strong>Purchase tool</strong>
      <%= render 'purchases/form', tool: @tool, purchase: @purchase %>
  </div> -->
  <!-- <div id="tool-feedback">
    <strong>Add a feedback</strong>
    <p><strong>See all the reviews</strong>
      <% if @tool_feedbacks != nil %>
      <% @tool_feedbacks.each_with_index do |tool_feedback, index| %>
        <p><strong>Feedback n°<%=index + 1 %></strong></p>
        <p><strong>From: </strong><%= tool_feedback.user.first_name %> <%= tool_feedback.user.last_name %></p>
        <p> <%= tool_feedback.content %> </p>
      <% end %>
      <%else %>
        <p>no feedback yet</p>
      <% end %>
    </p>
  </div>
  <div class="update-tool">
  </div>
  <div class="delete-tool">
    <%= link_to "Delete tool", tool_path(@tool), method: :delete %>
  </div>
</div>
 -->
